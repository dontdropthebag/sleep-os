# URL of the deployed backend API (FastAPI). Public (baked into the browser
# bundle at build time) — this is the base URL only, never a secret.
# Local dev default is http://localhost:8000
NEXT_PUBLIC_API_URL=https://your-backend-host.example.com
