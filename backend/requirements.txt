# Production runtime dependencies.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
# Postgres driver (production database on Vercel/Render/Railway/Supabase/Neon).
# SQLite (local dev) needs no extra driver.
psycopg[binary]>=3.2
